import { fontColor, hoverColor } from "./color";
const cssVars = {
  "--primary-color": fontColor.blue,
  "--error-font-color": fontColor.errorColor,
  "--primary-color-darker": hoverColor.hoverBlue,
};

export default cssVars;
